# Shared defaults applied to all clients.
# Copy to defaults.yaml and fill in your values.
# Client configs override or extend these values.
# Lists (business.data_notes, business.lead_rules, notes, etc.) are appended.

business:
  lead_rules:
    meta:
      include_action_types:
        - "messaging_conversation_started_7d"
        - "onsite_conversion.messaging_conversation_started_7d"
    google_ads:
      include_conversion_actions: []
      exclude_conversion_actions: []
