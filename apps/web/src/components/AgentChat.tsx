import { Card } from "~/components/Card";

export function AgentChat() {
  return (
    <Card style={{ padding: 20 }}>
      <div style={{ fontSize: 14 }}>
        Chat moved into the timeline input on <code>AgentWorkspace</code>.
      </div>
    </Card>
  );
}

