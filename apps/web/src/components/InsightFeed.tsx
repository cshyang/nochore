import { Card } from "~/components/Card";

export function InsightFeed() {
  return (
    <Card style={{ padding: 20 }}>
      <div style={{ fontSize: 14 }}>
        InsightFeed has been replaced by the timeline in <code>AgentWorkspace</code>.
      </div>
    </Card>
  );
}
