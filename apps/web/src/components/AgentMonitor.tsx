import { Card } from "~/components/Card";

export function AgentMonitor() {
  return (
    <Card style={{ padding: 20 }}>
      <div style={{ fontSize: 14 }}>AgentMonitor is no longer a separate surface.</div>
    </Card>
  );
}
