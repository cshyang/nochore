import { Card } from "~/components/Card";

export function AgentDetail() {
  return (
    <Card style={{ padding: 20 }}>
      <div style={{ fontSize: 14, color: "inherit" }}>
        AgentDetail has been folded into <code>AgentWorkspace</code>.
      </div>
    </Card>
  );
}

