import { Card } from "~/components/Card";

export function MemoryTimeline() {
  return (
    <Card style={{ padding: 20 }}>
      <div style={{ fontSize: 14 }}>MemoryTimeline is folded into the agent settings dossier.</div>
    </Card>
  );
}
