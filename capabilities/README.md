# Capability Catalog

`capabilities/` is the canonical project-owned catalog for reusable agent capabilities.

- `skills/` contains active project skill definitions that are loaded at runtime
- `agents/` contains reusable agent definitions for delegation and composition
- `prompts/` contains authored prompt templates
- `incubator/` holds non-canonical capability drafts that are intentionally excluded from runtime discovery

`.claude/` remains a local tooling surface and is not part of runtime discovery.
