# Capability Incubator

`capabilities/incubator/` stores draft or imported capability content that is not part of the live catalog.

- Nothing under this directory is loaded by the runtime catalog.
- Promote a capability into `capabilities/skills/`, `capabilities/agents/`, or `capabilities/prompts/` only after it has been intentionally curated.
